# Reference outputs

Drop known-good outputs from the classifier here. `build_snapshots.py`
dry-run diffs against them as the Phase A verification gate.

Expected files:
- `headline_numbers.json`
- `comparator_segment_shares.csv`

Both are committed to the repo so verification is reproducible.
